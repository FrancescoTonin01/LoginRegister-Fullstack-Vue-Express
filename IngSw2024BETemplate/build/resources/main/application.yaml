spring:
  application:
    name: MicroservizioBase
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    password: root
    url: jdbc:mysql://localhost:3306/microservices
    username: root

  jpa:
    hibernate:
      ddl-auto:
        update
      dialect:
        org.hibernate.dialect.MySQL8Dialect

server:
  port: 8081
